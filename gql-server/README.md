# NHL Scoreboard - GraphQL Server

## Install
```
yarn install
```

## Usage
To run the GraphQL server, first run the development build, which also watches the files.
```
NODE_ENV=development npm run build
```

In a new terminal, then run:
```
yarn start:env
```
